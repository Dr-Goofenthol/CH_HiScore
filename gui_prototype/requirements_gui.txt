# GUI Prototype Requirements
# Additional dependencies needed for the CustomTkinter GUI version

# Modern UI framework (based on Tkinter)
customtkinter>=5.0.0

# Already included in main project requirements.txt:
# - watchdog>=3.0.0
# - requests>=2.31.0
# - Pillow>=10.0.0
# - pystray>=0.19.0
# - colorama>=0.4.6

# Note: Run this from the project root to install GUI dependencies:
# pip install -r gui_prototype/requirements_gui.txt
#
# Or if you want all dependencies:
# pip install -r requirements.txt
# pip install customtkinter
